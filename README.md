## TODO: Fix NullPointerException in Auth Service

This is a placeholder to create a pull request for JIRA KAN-6. The actual fix for the `NullPointerException` in `AuthController.java` and `LoginFilter.java` needs to be applied in the separate authentication service repository, not in `pvaranasi-winfo/disc-ui`.

The `disc-ui` repository is a UI project and does not contain the Java backend code for the authentication service.