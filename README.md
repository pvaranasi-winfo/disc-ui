## TODO: Fix memory leak in background worker service

The `EmailQueueProcessor.java` file was not found in this repository (`pvaranasi-winfo/disc-ui`). This fix needs to be applied to the correct repository containing the background worker service. The JIRA ticket KAN-12 describes a memory leak in `EmailQueueProcessor.java` where `attachmentCache` is not cleared. The proposed fix is to add `attachmentCache.clear()` at the end of `processQueue()` or replace it with a Caffeine bounded LRU cache.